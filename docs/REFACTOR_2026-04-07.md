# Refactor 2026-04-07 — CLI religado ao Orchestrator v2.0

**Branch**: `refactor/cli-orchestrator-v2`
**Escopo**: refatoracao atomica do `cli.py` para eliminar o caminho legacy
v1.0 e expor a infraestrutura v2.0 que ja existia mas estava dead code.
**Smoke test**: 20/20 tests passando, todos os comandos do CLI registrados,
import limpo, `status` ja surfaceia alerta FinOps.

## Motivacao

A auditoria executada em 2026-04-07 (resposta a uma execucao do
`geo orchestrator run` que terminou com um diagnostico preocupante do agente
de execucao) confirmou tres problemas severos:

1. **Dois caminhos paralelos no codigo**:
   - `Orchestrator.run()` (v2.0): SmartRouter + cap 80% + quality gates +
     semantic cache + code-first + checkpoint + fallback chain estruturada
     + FinOps por tarefa.
   - `cli.py:_execute_plan()` (v1.0): instancia agents diretamente, sem
     nenhuma das features acima.
   
   O `cli.py run` chamava o caminho v1.0. Toda a v2.0 era dead code no fluxo
   do usuario.

2. **Concentracao de carga em Claude**: a execucao 20260407_180740 mostrou
   12/12 tarefas indo para `ArchitectAgent` (Claude). Gasto Anthropic do dia
   chegou a US$ 4.969 / US$ 5.00 (99.4% do teto local). Cap 80% nao rodou
   porque vivia no caminho v2.0.

3. **Documentacao falsa**: `CLAUDE.md` descrevia a v2.0 como ativa ha um mes,
   quando na pratica o CLI continuava no legacy.

## O que mudou

### `cli.py` — refatoracao atomica

| Antes | Depois |
|---|---|
| `from src.agents.base import TaskResult, ...` | `from src.models import ExecutionReport, Plan, TaskResult` |
| `from src.templates.decomposition import DECOMPOSITION_PROMPT` | `from src.orchestrator import Orchestrator` |
| `from src.agents.researcher import ResearcherAgent` (e 4 outros agents) | `from src.pipeline import Pipeline; from src.smart_router import SmartRouter` |
| `MODELS = {...}` (9 entradas hardcoded) | `from src.config import LLM_CONFIGS` |
| `_select_claude_by_complexity` / `_select_groq_by_complexity` | removidos (tier routing vive no Router) |
| `_get_httpx_client`, `_create_agent`, `_decompose_demand`, `_execute_plan` | **removidos** (~290 linhas legacy) |
| `_display_plan(dict)`, `_display_summary(list[TaskResult])`, `_save_report(plan, results)` | reescritos para `Plan` / `ExecutionReport` (v2.0 shapes) |
| `run()` chamava `_decompose_demand` + `_execute_plan` | `run()` chama `Orchestrator(smart=True).run()` |
| Sem comando `resume` | `cli.py resume [-c <checkpoint>]` chama `Pipeline.resume()` |
| `status` itera `MODELS` legacy | `status` itera `LLM_CONFIGS` + alerta FinOps |
| `models` itera `MODELS` legacy | `models` itera `LLM_CONFIGS` |

### Tamanho

- **Antes**: 886 linhas (`cli.py`), com 290+ linhas de legacy.
- **Depois**: 759 linhas, ~127 linhas removidas e ~150 reescritas em torno
  dos tipos canonicos da v2.0.

### Novos flags do `run`

- `--force` — bypass do `BUDGET_LIMIT` (US$ 5.00 por execucao por padrao)
- `--no-smart` — desliga o SmartRouter (debug; volta ao Router classico)
- `--dry-run` — agora usa `Orchestrator.decompose()` e salva o `Plan`
  serializado em `output/plan_<ts>.json`

### Comando `resume` (novo)

```bash
python cli.py resume                                    # usa output/.checkpoint.json
python cli.py resume -c output/checkpoints/ckpt.json    # caminho explicito
python cli.py resume --no-smart                         # debug
```

Internamente cria `SmartRouter()` (ou `Router()` se `--no-smart`) e chama
`Pipeline.resume(checkpoint_path, router)`. O checkpoint e gravado pelo
proprio Pipeline a cada wave em `output/.checkpoint.json`. Em caso de crash,
timeout, Ctrl-C ou queda de provider, basta `python cli.py resume` para
continuar do ultimo wave salvo sem reexecutar tarefas concluidas.

### `status` agora surfaceia alerta FinOps

Alem da tabela canonica de LLMs (via `LLM_CONFIGS`), o comando `status`
agora le `FinOps.daily_status()` e imprime aviso quando algum provider
passa de 80% do limite diario. Isso teria evitado a execucao 20260407_180740
ter ido toda para Claude — o operador veria `BLOQUEADO anthropic` antes de
disparar o run.

### Schema do `output/execution_*.json` mudou

Antes (v1.0):
```json
{
  "plan": {"demand_summary": "...", "tasks": [...], "execution_plan": {...}},
  "results": [{"task_id": "...", "agent_name": "...", "model_used": "...", "cost_usd": ...}],
  "totals": {"cost_usd": ..., "duration_seconds": ...}
}
```

Agora (v2.0):
```json
{
  "demand": "...",
  "summary": "...",
  "totals": {
    "cost_usd": ..., "estimated_cost_usd": ..., "duration_ms": ...,
    "tasks_completed": ..., "tasks_failed": ..., "tasks_cached": ...,
    "tasks_deduplicated": ..., "tasks_quality_retried": ...,
    "budget_limit": ...
  },
  "plan": <Plan.model_dump()>,
  "results": {"<task_id>": <TaskResult.model_dump()>}
}
```

Scripts externos que consomem o formato antigo (`scripts/run_5llm_board.py`,
dashboards, geo-finops sync) precisam ser ajustados — backlog em **Roadmap**
abaixo.

## Gaps fechados (vs auditoria de 2026-04-07)

| # | Gap original | Status |
|---|---|---|
| 1 | CLI nao usa SmartRouter | FECHADO — `Orchestrator(smart=True).run()` |
| 2 | Cap 80% nao roda no CLI | FECHADO — Pipeline aplica via `_apply_concentration_cap` |
| 3 | Quality gates ausentes no CLI | FECHADO — Pipeline `_quality_check` ativo |
| 4 | Quality Judge nao usado | FECHADO — Orchestrator chama `QualityJudge.evaluate` |
| 5 | Fallback chain estruturada ausente | FECHADO — Pipeline itera `FALLBACK_CHAINS` |
| 6 | FinOps por tarefa ausente | FECHADO — Pipeline chama `finops.check_budget()` antes de cada call |
| 7 | Semantic Cache nao usado | FECHADO — Orchestrator usa `SemanticCache` |
| 8 | Code-First Gate nao usado | FECHADO — Orchestrator chama `try_code_first` |
| 9 | Checkpoint/Resume sem comando CLI | FECHADO — comando `cli.py resume` adicionado |
| 10 | Timeout unico (300s) para tudo | FECHADO — Pipeline usa `TIMEOUT_BY_TASK_TYPE` |
| 11 | Tier routing duplicado em `cli.py` (1-5 ints) e `config.py` (low/medium/high) | FECHADO — `cli.py` nao mais define tier; usa `Router._get_tier_candidates()` via `MODEL_TIERS` |

## Gaps remanescentes (proximo roadmap)

| # | Gap | Prioridade | Esforco |
|---|---|---|---|
| 12 | Decomposer pode rotular "criar helper Python" como `architecture` -> `ArchitectAgent` (prompt Next.js/Cloudflare). Separar `code_generation` de `architecture` em `TASK_TYPES` e em `DECOMPOSE_SYSTEM`. | ALTA | 2h |
| 13 | Sanitizacao de paths/slugs com acento. Reusar licao do incidente 2026-03-27. Helper unico `sanitize_path()` em `src/code_executor.py` e `src/agents/architect.py`. | ALTA | 1h |
| 14 | Testes de integracao para Pipeline (`tests/test_pipeline_execute.py`), SmartRouter (`tests/test_smart_router.py`), CLI E2E (`tests/test_cli_e2e.py`). Hoje so existe `test_core.py` com 20 testes de unidade. | MEDIA | 4h |
| 15 | Consumidores externos do `output/execution_*.json` (run_5llm_board, dashboards, geo-finops sync) precisam adaptar ao novo schema. | MEDIA | 2h |
| 16 | Flag `--force-5-llm` em `cli.py run` para garantir distribuicao em 5 LLMs em demandas sensiveis (chama `router.force_all_models_route` explicitamente). Util para QA. | BAIXA | 30min |
| 17 | Tier downgrade dentro da familia Claude (Opus/Sonnet/Haiku por complexity). Hoje `LLM_CONFIGS["claude"]` e sempre Opus. Adicionar `claude_sonnet`, `claude_haiku` e regra de downgrade. | BAIXA | 3h |
| 18 | Limite diario Anthropic em `src/config.py` esta em US$ 2.00 mas o status mostrou US$ 4.97 ja gasto sem bloqueio efetivo (o pre_check so dispara em pre-execucao). Investigar se FinOps esta apenas alertando ou bloqueando de fato. | ALTA | 1h |

## Sprint #3 — fechada 2026-04-07 (8 fixes + tier interno acionou em runtime)

**Sumario**: Run #5 = US$ 0.0727 / 97.5s / 8 tasks / 5/5 LLMs / 75/75 tests.
Comparado ao run #1 original (legacy v1.0): **−89% custo, −60% duracao**.

| # | Fix | Arquivo | Status |
|---|---|---|---|
| 11 | Sanitizacao ASCII de paths | `src/sanitize.py` (NOVO) + `pipeline._save_task_result` | RESOLVIDO + 9 tests |
| 13 | Decomposer marca complexity variavel | `orchestrator._estimate_complexity` refinado | RESOLVIDO + 5 tests + RUNTIME |
| 14 | KPI history persistido | `src/kpi_history.py` (NOVO) + `Orchestrator.run()` | RESOLVIDO + 4 tests |
| 15 | Drift alert automatico | `kpi_history.detect_drift()` | RESOLVIDO + 3 tests |
| 16 | `cli.py dashboard` | Novo @cli.command() | RESOLVIDO + 2 tests |
| Bonus | Display Anthropic agrega Sonnet/Haiku | `cli._display_summary` | RESOLVIDO |
| Bonus | Race condition session_usage 2x | `router.route()` + `pipeline.execute()` reset | RESOLVIDO |
| Bonus | Parser task IDs robusto (regex) | `orchestrator._validate_balance` | RESOLVIDO |

### Marco: tier interno Claude em runtime

t7 review do run #5 foi para `claude_haiku` automaticamente (-95% custo vs Opus).
O decomposer refinado defaulta code/review como MEDIUM (antes: HIGH), aplica
lexical override e usa thresholds maiores. O Router.downgrade_claude_by_complexity
decide automaticamente. **Validado por 5 testes unitarios + evidencia empirica
no run #5**.

### KPI History + Drift detection em producao

`output/.kpi_history.jsonl` agora persiste:
- `distribution_health` = (used_llms/5) * (1 - max(0, max_share - 0.8))
- `cost_estimate_accuracy` = real_cost / estimated_cost
- llm_usage, total tasks, custo, duracao

`detect_drift()` dispara alerta se 3 runs consecutivos saem da banda 0.7-1.5x.
Sprint 3 ja gerou 2 runs com accuracy=0.13/0.24 — proxima execucao igual
dispara o alerta no campo `summary` do ExecutionReport.

### Sprint #4 — proximo backlog

| # | Item | Prio |
|---|---|---|
| 19 | **Recalibrar AVG_COST_PER_CALL** — sprint 3 baixou tanto o custo (tier interno) que estimativa virou drift PARA BAIXO. AVG agora deve refletir mix Opus/Sonnet/Haiku ponderado por frequencia. | P0 |
| 20 | Catalog YAML como source of truth unica para model_ids | P1 |
| 21 | Decomposer pode usar Sonnet em vez de Opus para o proprio decompose() | P2 |
| 22 | Adaptar consumidores externos do `output/execution_*.json` | P1 |
| 23 | Implementar sub-decomposicao real do review final | P1 |
| 24 | KPI `tier_internal_engagement_rate` = % de tarefas Claude que foram para Sonnet/Haiku | P1 |
| 25 | KPI `fallback_chain_save_rate` = % de runs onde fallback chain salvou pelo menos 1 task | P1 |
| 26 | `cli.py dashboard --export csv` para Looker/Metabase | P2 |

## Roadmap atualizado do geo-orchestrator

### Sprint atual (2026-04-07 a 2026-04-14)

- [x] **CLI religado ao Orchestrator v2.0** (este documento)
- [x] **Comando `resume` exposto**
- [x] **Dedup tier routing** (`cli.py` -> `LLM_CONFIGS`)
- [x] **Status com alerta FinOps**
- [ ] **Gap #18**: investigar pre_check FinOps Anthropic vs gasto real
- [ ] **Gap #12**: separar `code_generation` de `architecture` no decomposer
- [ ] **Gap #13**: sanitizacao ASCII de paths/slugs gerados

### Sprint +1 (2026-04-14 a 2026-04-21)

- [ ] **Gap #14**: bateria de testes de integracao (Pipeline, SmartRouter, CLI E2E)
- [ ] **Gap #15**: adaptar consumidores do `output/execution_*.json`
- [ ] **Gap #16**: flag `--force-5-llm`

### Backlog

- [ ] **Gap #17**: tier interno Claude (Opus/Sonnet/Haiku)
- [ ] Migrar `scripts/run_5llm_board.py` para usar `Orchestrator` diretamente
- [ ] Substituir `tests/test_core.py` (unidade) por suite mais ampla com Pipeline ponta a ponta
- [ ] Documentar regras de cap por provider em `governance/` para alinhar com `caramaschi/`
- [ ] Adicionar dashboard CLI: `cli.py dashboard` mostrando waves, custo, distribuicao, cap em tempo real (v2.0 ja imprime, mas via logger — expor como tabela rica)

## Validacao

```bash
$ cd geo-orchestrator
$ python -m pytest tests/ -x --tb=short
============================= 20 passed in 0.05s ==============================

$ python cli.py --help
Commands:
  cost-report  Mostra historico de custos.
  finops       Governanca FinOps - limites diarios, gastos e relatorios.
  models       Lista os 5 LLMs canonicos (LLM_CONFIGS - source of truth).
  plan         Decompoe a demanda via Orchestrator v2.0 e mostra o plano...
  resume       Retoma uma execucao interrompida a partir de um checkpoint.
  run          Executa o pipeline v2.0 completo para uma demanda.
  status       Mostra LLMs canonicos (LLM_CONFIGS) e seu status - alem do...
  trace        Distributed tracing - visualizar execucoes passadas.

$ python cli.py status
... tabela LLM_CONFIGS ...
FinOps - providers proximos do limite:
  BLOQUEADO anthropic: US$ 4.9690 / 2.00 (248%)   # exatamente o que a auditoria pediu
```

## Como usar pos-refatoracao

```bash
# Run completo (caminho v2.0 com todas as protecoes)
python cli.py run "criar helper Python para validar acentos em URLs"

# Dry-run (so decomposicao, sem custo de execucao)
python cli.py run "<demanda>" --dry-run

# Forcar bypass do budget guard
python cli.py run "<demanda>" --force

# Retomar execucao interrompida
python cli.py resume

# Status canonico + alerta FinOps
python cli.py status

# Listar 5 LLMs canonicos
python cli.py models
```
